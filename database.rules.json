{
  "rules": {
    "userTokens": {
      "$uid": {
        ".read": "auth !== null && auth.uid === $uid",
        ".write": "auth !== null && auth.uid === $uid",
        ".validate": "newData.isString() && newData.val().length > 10"
      }
    },
    "txInbox": {
      "$token": {
        ".read": "auth !== null && root.child('userTokens').child(auth.uid).val() === $token",
        "$pushId": {
          ".write": true,
          ".validate": "newData.hasChildren(['amount', 'type', 'category']) && newData.child('amount').isNumber() && newData.child('amount').val() > 0 && (newData.child('type').val() === 'income' || newData.child('type').val() === 'expense') && newData.child('category').isString() && (newData.child('note').val() === null || newData.child('note').isString())"
        }
      }
    },
    "catList": {
      "$token": {
        ".read": true,
        ".write": "auth !== null && root.child('userTokens').child(auth.uid).val() === $token"
      }
    }
  }
}
